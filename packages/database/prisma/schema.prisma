generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                                              String                    @id @default(cuid())
  email                                           String                    @unique
  username                                        String                    @unique
  passwordHash                                    String
  role                                            UserRole                  @default(PATIENT)
  verified                                        Boolean                   @default(false)
  doctorVerificationStatus                        DoctorVerificationStatus?
  medicalLicenseNumber                            String?
  licenseIssuingAuthority                         String?
  licenseExpiryDate                               DateTime?
  specialty                                       String?
  subSpecialty                                    String?
  yearsOfExperience                               Int?
  hospitalAffiliation                             String?
  clinicAddress                                   String?
  phone                                           String?
  kycDocuments                                    Json?
  verificationDocuments                           Json?
  verificationNotes                               String?
  verifiedAt                                      DateTime?
  verifiedBy                                      String?
  rejectionReason                                 String?
  bio                                             String?
  avatar                                          String?
  banner                                          String?
  postKarma                                       Int                       @default(0)
  commentKarma                                    Int                       @default(0)
  totalKarma                                      Int                       @default(0)
  coins                                           Int                       @default(0)
  isPremium                                       Boolean                   @default(false)
  isNSFW                                          Boolean                   @default(false)
  isSuspended                                     Boolean                   @default(false)
  isShadowBanned                                  Boolean                   @default(false)
  emailVerified                                   Boolean                   @default(false)
  twoFactorEnabled                                Boolean                   @default(false)
  createdAt                                       DateTime                  @default(now())
  updatedAt                                       DateTime                  @updatedAt
  graduationYear                                  Int?
  medicalUniversity                               String?
  pincode                                         String?
  city                                            String?
  district                                        String?
  state                                           String?
  AnalyticsEvent                                  AnalyticsEvent[]
  appointmentsAsDoctor                            Appointment[]             @relation("DoctorAppointments")
  appointmentsAsPatient                           Appointment[]             @relation("PatientAppointments")
  auditLogs                                       AuditLog[]                @relation("AdminAuditLogs")
  availabilities                                  Availability[]
  givenAwards                                     AwardGiven[]
  blockedBy                                       Block[]                   @relation("Blocked")
  blockedUsers                                    Block[]                   @relation("Blocker")
  timelineEvents                                  CaseTimelineEvent[]
  comments                                        Comment[]
  doctorConversions                               CommentConversion[]       @relation("DoctorConversions")
  patientConversions                              CommentConversion[]       @relation("PatientConversions")
  communities                                     CommunityMember[]
  moderating                                      CommunityModerator[]
  ConsultationFee_ConsultationFee_doctorIdToUser  ConsultationFee[]         @relation("ConsultationFee_doctorIdToUser")
  ConsultationFee_ConsultationFee_patientIdToUser ConsultationFee[]         @relation("ConsultationFee_patientIdToUser")
  contentModerations                              ContentModeration[]
  ConversionEvent                                 ConversionEvent[]
  dietPlans                                       DietPlan[]
  doctorActivityMetrics                           DoctorActivityMetrics?
  doctorEndorsements                              DoctorEndorsement[]       @relation("DoctorEndorsements")
  emergencyBroadcasts                             EmergencyBroadcast[]      @relation("EmergencyBroadcastCreator")
  following                                       Follow[]                  @relation("Following")
  followers                                       Follow[]                  @relation("Followers")
  forumAnswers                                    ForumAnswer[]             @relation("ForumAnswers")
  forumQuestions                                  ForumQuestion[]           @relation("ForumQuestions")
  healthProfile                                   HealthProfile?
  hiddenPosts                                     HiddenPost[]
  medicalThreads                                  MedicalThread[]           @relation("PatientThreads")
  receivedMessages                                Message[]                 @relation("Receiver")
  messages                                        Message[]                 @relation("Sender")
  PageView                                        PageView[]
  doctorFeedbacks                                 PatientFeedback[]         @relation("DoctorFeedbacks")
  patientFeedbacks                                PatientFeedback[]         @relation("PatientFeedbacks")
  patientHealthProfile                            PatientHealthProfile?
  Payment                                         Payment[]
  PaymentHistory                                  PaymentHistory[]
  posts                                           Post[]
  Refund                                          Refund[]
  reports                                         Report[]                  @relation("UserReports")
  savedComments                                   SavedComment[]
  savedPosts                                      SavedPost[]
  searchHistory                                   SearchHistory[]
  storyComments                                   StoryComment[]            @relation("StoryComments")
  Subscription                                    Subscription[]
  successStories                                  SuccessStory[]            @relation("SuccessStories")
  symptomReports                                  SymptomReport[]
  threadReplies                                   ThreadReply[]
  userActivityLogs                                UserActivityLog[]
  UserAnalytics                                   UserAnalytics?
  devices                                         UserDevice[]
  UserSession                                     UserSession[]
  votes                                           Vote[]
  email_queue                                     email_queue[]
  notification_preferences                        notification_preferences?
  notifications_notifications_actorIdToUser       notifications[]           @relation("notifications_actorIdToUser")
  notifications_notifications_recipientIdToUser   notifications[]           @relation("notifications_recipientIdToUser")
  conversations                                   Conversation[]            @relation("ConversationParticipants")

  @@index([email])
  @@index([username])
  @@index([totalKarma])
}

model Community {
  id           String               @id @default(cuid())
  name         String               @unique
  displayName  String
  description  String?
  icon         String?
  banner       String?
  theme        Json?
  rules        Json?
  isNSFW       Boolean              @default(false)
  isPrivate    Boolean              @default(false)
  isRestricted Boolean              @default(false)
  memberCount  Int                  @default(0)
  createdAt    DateTime             @default(now())
  updatedAt    DateTime             @updatedAt
  activity     CommunityActivity?
  members      CommunityMember[]
  moderators   CommunityModerator[]
  flairs       Flair[]
  posts        Post[]

  @@index([name])
  @@index([memberCount])
}

model Post {
  id               String              @id @default(cuid())
  type             PostType            @default(TEXT)
  title            String
  content          String?
  url              String?
  mediaUrls        String[]
  thumbnailUrl     String?
  authorId         String
  communityId      String
  flairId          String?
  upvotes          Int                 @default(0)
  downvotes        Int                 @default(0)
  score            Int                 @default(0)
  commentCount     Int                 @default(0)
  isNSFW           Boolean             @default(false)
  isSpoiler        Boolean             @default(false)
  isPinned         Boolean             @default(false)
  isLocked         Boolean             @default(false)
  isArchived       Boolean             @default(false)
  isRemoved        Boolean             @default(false)
  isDraft          Boolean             @default(false)
  commentsDisabled Boolean             @default(false)
  contestMode      Boolean             @default(false)
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt
  editedAt         DateTime?
  publishedAt      DateTime?
  endorsementCount Int                 @default(0)
  isPrivate        Boolean             @default(false)
  awards           AwardGiven[]
  comments         Comment[]
  conversions      CommentConversion[]
  endorsements     DoctorEndorsement[]
  hiddenBy         HiddenPost[]
  author           User                @relation(fields: [authorId], references: [id])
  community        Community           @relation(fields: [communityId], references: [id])
  flair            Flair?              @relation(fields: [flairId], references: [id])
  PostAnalytics    PostAnalytics[]
  priority         PostPriority?
  reports          Report[]
  savedBy          SavedPost[]
  symptomReports   SymptomReport[]
  votes            Vote[]

  @@index([authorId])
  @@index([communityId])
  @@index([score])
  @@index([createdAt])
  @@index([isDraft])
}

model Comment {
  id              String              @id @default(cuid())
  content         String
  authorId        String
  postId          String
  parentId        String?
  upvotes         Int                 @default(0)
  downvotes       Int                 @default(0)
  score           Int                 @default(0)
  depth           Int                 @default(0)
  isStickied      Boolean             @default(false)
  isDistinguished Boolean             @default(false)
  isRemoved       Boolean             @default(false)
  isLocked        Boolean             @default(false)
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt
  editedAt        DateTime?
  awards          AwardGiven[]
  author          User                @relation(fields: [authorId], references: [id])
  parent          Comment?            @relation("CommentReplies", fields: [parentId], references: [id])
  replies         Comment[]           @relation("CommentReplies")
  post            Post                @relation(fields: [postId], references: [id], onDelete: Cascade)
  conversions     CommentConversion[]
  reports         Report[]
  savedBy         SavedComment[]
  votes           Vote[]

  @@index([postId])
  @@index([authorId])
  @@index([parentId])
  @@index([score])
}

model Vote {
  id        String   @id @default(cuid())
  userId    String
  postId    String?
  commentId String?
  value     Int
  createdAt DateTime @default(now())
  comment   Comment? @relation(fields: [commentId], references: [id], onDelete: Cascade)
  post      Post?    @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, postId])
  @@unique([userId, commentId])
  @@index([userId])
  @@index([postId])
  @@index([commentId])
}

model Flair {
  id          String    @id @default(cuid())
  text        String
  color       String?
  communityId String
  isUserFlair Boolean   @default(false)
  community   Community @relation(fields: [communityId], references: [id])
  posts       Post[]

  @@index([communityId])
}

model Award {
  id          String       @id @default(cuid())
  name        String       @unique
  description String?
  icon        String
  cost        Int
  tier        Int          @default(1)
  color       String       @default("#gray")
  createdAt   DateTime     @default(now())
  given       AwardGiven[]

  @@index([cost])
  @@index([tier])
}

model AwardGiven {
  id        String   @id @default(cuid())
  awardId   String
  giverId   String
  postId    String?
  commentId String?
  createdAt DateTime @default(now())
  award     Award    @relation(fields: [awardId], references: [id])
  comment   Comment? @relation(fields: [commentId], references: [id])
  giver     User     @relation(fields: [giverId], references: [id])
  post      Post?    @relation(fields: [postId], references: [id])

  @@index([postId])
  @@index([commentId])
  @@index([giverId])
}

model SavedPost {
  id        String   @id @default(cuid())
  userId    String
  postId    String
  createdAt DateTime @default(now())
  post      Post     @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, postId])
  @@index([userId])
}

model SavedComment {
  id        String   @id @default(cuid())
  userId    String
  commentId String
  createdAt DateTime @default(now())
  comment   Comment  @relation(fields: [commentId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, commentId])
  @@index([userId])
}

model HiddenPost {
  id        String   @id @default(cuid())
  userId    String
  postId    String
  createdAt DateTime @default(now())
  post      Post     @relation(fields: [postId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, postId])
  @@index([userId])
}

model CommunityMember {
  id          String    @id @default(cuid())
  userId      String
  communityId String
  joinedAt    DateTime  @default(now())
  community   Community @relation(fields: [communityId], references: [id])
  user        User      @relation(fields: [userId], references: [id])

  @@unique([userId, communityId])
  @@index([userId])
  @@index([communityId])
}

model CommunityModerator {
  id          String    @id @default(cuid())
  userId      String
  communityId String
  permissions Json
  addedAt     DateTime  @default(now())
  community   Community @relation(fields: [communityId], references: [id])
  user        User      @relation(fields: [userId], references: [id])

  @@unique([userId, communityId])
  @@index([communityId])
}

model Follow {
  id          String   @id @default(cuid())
  followerId  String
  followingId String
  createdAt   DateTime @default(now())
  follower    User     @relation("Following", fields: [followerId], references: [id])
  following   User     @relation("Followers", fields: [followingId], references: [id])

  @@unique([followerId, followingId])
  @@index([followerId])
  @@index([followingId])
}

model Block {
  id        String   @id @default(cuid())
  blockerId String
  blockedId String
  createdAt DateTime @default(now())
  blocked   User     @relation("Blocked", fields: [blockedId], references: [id])
  blocker   User     @relation("Blocker", fields: [blockerId], references: [id])

  @@unique([blockerId, blockedId])
  @@index([blockerId])
}

model Message {
  id             String        @id @default(cuid())
  senderId       String
  receiverId     String
  subject        String?
  content        String
  isRead         Boolean       @default(false)
  conversationId String?
  attachment     String?
  type           MessageType   @default(TEXT)
  createdAt      DateTime      @default(now())
  isUrgent       Boolean       @default(false)
  urgencyLevel   String?
  conversation   Conversation? @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  receiver       User          @relation("Receiver", fields: [receiverId], references: [id], onDelete: Cascade)
  sender         User          @relation("Sender", fields: [senderId], references: [id], onDelete: Cascade)

  @@index([senderId])
  @@index([receiverId])
}

model Report {
  id        String   @id @default(cuid())
  userId    String
  postId    String?
  commentId String?
  reason    String
  details   String?
  status    String   @default("PENDING")
  createdAt DateTime @default(now())
  comment   Comment? @relation(fields: [commentId], references: [id])
  post      Post?    @relation(fields: [postId], references: [id])
  user      User     @relation("UserReports", fields: [userId], references: [id])

  @@index([userId])
  @@index([status])
}

model Availability {
  id        String   @id @default(cuid())
  doctorId  String
  dayOfWeek Int
  startTime DateTime
  endTime   DateTime
  isBooked  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  doctor    User     @relation(fields: [doctorId], references: [id])

  @@index([doctorId])
}

model Appointment {
  id             String            @id @default(cuid())
  patientId      String
  doctorId       String
  startTime      DateTime
  endTime        DateTime
  status         AppointmentStatus @default(PENDING)
  reason         String?
  createdAt      DateTime          @default(now())
  updatedAt      DateTime          @updatedAt
  chiefComplaint String?
  diagnosisTag   String?
  notes          String?
  doctor         User              @relation("DoctorAppointments", fields: [doctorId], references: [id])
  patient        User              @relation("PatientAppointments", fields: [patientId], references: [id])
  conversation   Conversation?
  feedback       PatientFeedback[]

  @@index([patientId])
  @@index([doctorId])
}

model Conversation {
  id            String            @id @default(cuid())
  appointmentId String?           @unique
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
  appointment   Appointment?      @relation(fields: [appointmentId], references: [id])
  messages      Message[]
  feedback      PatientFeedback[]
  participants  User[]            @relation("ConversationParticipants")
}

model MedicalThread {
  id            String              @id @default(cuid())
  patientId     String
  title         String
  symptoms      Json
  severityScore String?
  tags          String[]
  status        String              @default("OPEN")
  isResolved    Boolean             @default(false)
  createdAt     DateTime            @default(now())
  updatedAt     DateTime            @updatedAt
  timeline      CaseTimelineEvent[]
  patient       User                @relation("PatientThreads", fields: [patientId], references: [id])
  replies       ThreadReply[]

  @@index([patientId])
  @@index([createdAt])
  @@index([status])
}

model ThreadReply {
  id             String        @id @default(cuid())
  threadId       String
  parentReplyId  String?
  authorId       String
  authorRole     UserRole
  content        String
  doctorVerified Boolean       @default(false)
  upvotes        Int           @default(0)
  downvotes      Int           @default(0)
  isHelpful      Boolean       @default(false)
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  author         User          @relation(fields: [authorId], references: [id])
  parentReply    ThreadReply?  @relation("ReplyReplies", fields: [parentReplyId], references: [id])
  childReplies   ThreadReply[] @relation("ReplyReplies")
  thread         MedicalThread @relation(fields: [threadId], references: [id], onDelete: Cascade)

  @@index([threadId])
  @@index([authorId])
  @@index([parentReplyId])
}

model CaseTimelineEvent {
  id        String        @id @default(cuid())
  threadId  String
  userId    String
  eventType String
  data      Json
  timestamp DateTime      @default(now())
  thread    MedicalThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  user      User          @relation(fields: [userId], references: [id])

  @@index([threadId])
  @@index([userId])
  @@index([timestamp])
}

model AuditLog {
  id         String      @id @default(cuid())
  action     AuditAction
  adminId    String
  targetType String?
  targetId   String?
  details    Json?
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime    @default(now())
  admin      User        @relation("AdminAuditLogs", fields: [adminId], references: [id])

  @@index([adminId])
  @@index([action])
  @@index([createdAt])
  @@index([targetType, targetId])
}

model AnalyticsEvent {
  id            String   @id
  eventName     String
  eventCategory String
  userId        String?
  sessionId     String
  properties    Json?
  timestamp     DateTime @default(now())
  ipAddress     String?
  userAgent     String?
  referrer      String?
  page          String?
  User          User?    @relation(fields: [userId], references: [id])

  @@index([eventName])
  @@index([sessionId])
  @@index([timestamp])
  @@index([userId])
}

model ConsultationFee {
  id                                   String        @id
  doctorId                             String
  appointmentId                        String?
  patientId                            String
  amount                               Decimal       @db.Decimal(10, 2)
  currency                             String        @default("USD")
  paymentId                            String?
  status                               PaymentStatus @default(PENDING)
  paidAt                               DateTime?
  createdAt                            DateTime      @default(now())
  updatedAt                            DateTime      @default(now())
  User_ConsultationFee_doctorIdToUser  User          @relation("ConsultationFee_doctorIdToUser", fields: [doctorId], references: [id], onDelete: Cascade)
  User_ConsultationFee_patientIdToUser User          @relation("ConsultationFee_patientIdToUser", fields: [patientId], references: [id], onDelete: Cascade)
  Payment                              Payment?      @relation(fields: [paymentId], references: [id])

  @@index([doctorId])
  @@index([patientId])
  @@index([paymentId])
  @@index([status])
}

model ConversionEvent {
  id             String   @id
  userId         String?
  sessionId      String
  conversionType String
  value          Decimal? @db.Decimal(10, 2)
  metadata       Json?
  timestamp      DateTime @default(now())
  User           User?    @relation(fields: [userId], references: [id])

  @@index([conversionType])
  @@index([timestamp])
  @@index([userId])
}

model PageView {
  id        String   @id
  userId    String?
  sessionId String
  page      String
  title     String?
  referrer  String?
  duration  Int?
  timestamp DateTime @default(now())
  User      User?    @relation(fields: [userId], references: [id])

  @@index([sessionId])
  @@index([timestamp])
  @@index([userId])
}

model Payment {
  id                    String            @id
  userId                String
  amount                Decimal           @db.Decimal(10, 2)
  currency              String            @default("USD")
  status                PaymentStatus     @default(PENDING)
  type                  PaymentType
  stripePaymentIntentId String?
  stripeChargeId        String?
  description           String?
  metadata              Json?
  createdAt             DateTime          @default(now())
  updatedAt             DateTime          @default(now())
  ConsultationFee       ConsultationFee[]
  User                  User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  PaymentHistory        PaymentHistory[]
  Refund                Refund[]

  @@index([createdAt])
  @@index([status])
  @@index([stripePaymentIntentId])
  @@index([type])
  @@index([userId])
}

model PaymentHistory {
  id          String   @id
  userId      String
  paymentId   String
  action      String
  amount      Decimal? @db.Decimal(10, 2)
  status      String?
  description String?
  createdAt   DateTime @default(now())
  Payment     Payment  @relation(fields: [paymentId], references: [id], onDelete: Cascade)
  User        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([createdAt])
  @@index([paymentId])
  @@index([userId])
}

model PostAnalytics {
  id           String   @id
  postId       String
  views        Int?     @default(0)
  uniqueViews  Int?     @default(0)
  clicks       Int?     @default(0)
  shares       Int?     @default(0)
  avgTimeSpent Int?     @default(0)
  bounceRate   Decimal? @default(0) @db.Decimal(5, 2)
  lastUpdated  DateTime @default(now())
  Post         Post     @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
}

model Refund {
  id             String       @id
  paymentId      String
  userId         String
  amount         Decimal      @db.Decimal(10, 2)
  reason         String
  status         RefundStatus @default(PENDING)
  stripeRefundId String?
  processedAt    DateTime?
  processedBy    String?
  adminNotes     String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @default(now())
  Payment        Payment      @relation(fields: [paymentId], references: [id], onDelete: Cascade)
  User           User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([paymentId])
  @@index([status])
  @@index([userId])
}

model Subscription {
  id                   String             @id
  userId               String
  planName             String
  planPrice            Decimal            @db.Decimal(10, 2)
  status               SubscriptionStatus @default(ACTIVE)
  stripeSubscriptionId String?
  stripeCustomerId     String?
  currentPeriodStart   DateTime
  currentPeriodEnd     DateTime
  cancelAtPeriodEnd    Boolean            @default(false)
  metadata             Json?
  createdAt            DateTime           @default(now())
  updatedAt            DateTime           @default(now())
  User                 User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([status])
  @@index([stripeSubscriptionId])
  @@index([userId])
}

model UserAnalytics {
  id              String    @id
  userId          String    @unique
  totalSessions   Int?      @default(0)
  totalPageViews  Int?      @default(0)
  totalTimeSpent  Int?      @default(0)
  postsCreated    Int?      @default(0)
  commentsCreated Int?      @default(0)
  lastActive      DateTime?
  lastUpdated     DateTime  @default(now())
  User            User      @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model UserSession {
  id        String    @id
  userId    String?
  startTime DateTime  @default(now())
  endTime   DateTime?
  duration  Int?
  pageViews Int?      @default(0)
  events    Int?      @default(0)
  ipAddress String?
  userAgent String?
  device    String?
  browser   String?
  os        String?
  User      User?     @relation(fields: [userId], references: [id])

  @@index([startTime])
  @@index([userId])
}

model email_queue {
  id             String        @id
  userId         String
  notificationId String
  type           String
  status         String        @default("pending")
  attempts       Int           @default(0)
  lastAttemptAt  DateTime?
  sentAt         DateTime?
  error          String?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime
  notifications  notifications @relation(fields: [notificationId], references: [id])
  User           User          @relation(fields: [userId], references: [id])

  @@index([status, createdAt])
  @@index([userId, type])
}

model notification_preferences {
  id              String   @id
  userId          String   @unique
  inApp           Json
  email           Json
  push            Json
  quietHoursStart String?
  quietHoursEnd   String?
  digestFrequency String   @default("daily")
  upvoteThreshold Int?
  createdAt       DateTime @default(now())
  updatedAt       DateTime
  User            User     @relation(fields: [userId], references: [id])
}

model notifications {
  id                                   String           @id
  type                                 NotificationType
  recipientId                          String
  actorId                              String
  contentId                            String?
  contentType                          ContentType?
  metadata                             Json
  isRead                               Boolean          @default(false)
  readAt                               DateTime?
  isDeleted                            Boolean          @default(false)
  deletedAt                            DateTime?
  createdAt                            DateTime         @default(now())
  updatedAt                            DateTime
  email_queue                          email_queue[]
  User_notifications_actorIdToUser     User             @relation("notifications_actorIdToUser", fields: [actorId], references: [id])
  User_notifications_recipientIdToUser User             @relation("notifications_recipientIdToUser", fields: [recipientId], references: [id])

  @@index([recipientId, isDeleted])
  @@index([recipientId, isRead, createdAt])
  @@index([recipientId, type])
}

model SymptomReport {
  id               String   @id @default(cuid())
  userId           String?
  sessionId        String?
  postId           String?
  symptoms         Json
  detectedSymptoms Json?
  location         Json?
  pincode          String?
  city             String?
  district         String?
  state            String?
  country          String?  @default("India")
  age              Int?
  gender           String?
  temperature      Float?
  duration         String?
  severity         String?
  metadata         Json?
  reportedAt       DateTime @default(now())
  createdAt        DateTime @default(now())
  post             Post?    @relation(fields: [postId], references: [id], onDelete: SetNull)
  user             User?    @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([createdAt])
  @@index([sessionId])
  @@index([pincode])
  @@index([city])
  @@index([district])
  @@index([state])
  @@index([reportedAt])
  @@index([severity])
}

model HealthTrend {
  id             String   @id @default(cuid())
  symptom        String
  count          Int
  region         String?
  severity       String?
  trendDirection String?
  percentChange  Float?
  timeWindow     String
  calculatedAt   DateTime @default(now())
  metadata       Json?

  @@index([symptom])
  @@index([region])
  @@index([calculatedAt])
}

model DoctorPerformance {
  id                    String    @id @default(cuid())
  doctorId              String    @unique
  totalResponses        Int       @default(0)
  totalPatientsHelped   Int       @default(0)
  avgResponseTime       Int?
  helpfulnessScore      Float?
  totalRatings          Int       @default(0)
  appointmentsCompleted Int       @default(0)
  appointmentsCancelled Int       @default(0)
  activeEngagementScore Float?
  lastActiveAt          DateTime?
  calculatedAt          DateTime  @default(now())
  metadata              Json?
  totalPostsCommented   Int       @default(0)
  totalCommentsCount    Int       @default(0)
  conversionCount       Int       @default(0)
  curedPatientCount     Int       @default(0)
  notYetCount           Int       @default(0)
  consultNewDoctorCount Int       @default(0)
  portfolioScore        Float     @default(0)
  clinicVisitCount      Int       @default(0)
  postClinicCureCount   Int       @default(0)

  @@index([doctorId])
  @@index([helpfulnessScore])
  @@index([activeEngagementScore])
  @@index([curedPatientCount])
  @@index([portfolioScore])
}

model PatientOutcome {
  id                String   @id @default(cuid())
  patientId         String
  doctorId          String?
  threadId          String?
  appointmentId     String?
  initialSymptoms   Json
  outcome           String
  recoveryTime      Int?
  followUpCount     Int      @default(0)
  satisfactionScore Float?
  feedback          String?
  createdAt         DateTime @default(now())
  metadata          Json?

  @@index([patientId])
  @@index([doctorId])
  @@index([outcome])
  @@index([createdAt])
}

model PlatformMetrics {
  id                  String   @id @default(cuid())
  date                DateTime @unique
  totalUsers          Int      @default(0)
  activeUsers         Int      @default(0)
  newUsers            Int      @default(0)
  totalDoctors        Int      @default(0)
  activeDoctors       Int      @default(0)
  newDoctors          Int      @default(0)
  totalPosts          Int      @default(0)
  totalAppointments   Int      @default(0)
  totalSymptomReports Int      @default(0)
  avgResponseTime     Int?
  peakUsageHour       Int?
  userRetentionRate   Float?
  doctorRetentionRate Float?
  metadata            Json?

  @@index([date])
}

model GeographicHealthData {
  id             String   @id @default(cuid())
  region         String
  latitude       Float?
  longitude      Float?
  topSymptoms    Json
  totalReports   Int      @default(0)
  alertLevel     String?
  trendingIssues Json?
  calculatedAt   DateTime @default(now())
  metadata       Json?

  @@index([region])
  @@index([alertLevel])
  @@index([calculatedAt])
}

model DoctorRating {
  id            String   @id @default(cuid())
  doctorId      String
  patientId     String
  appointmentId String?
  threadId      String?
  rating        Float
  helpfulness   Int?
  communication Int?
  expertise     Int?
  feedback      String?
  createdAt     DateTime @default(now())

  @@index([doctorId])
  @@index([patientId])
  @@index([createdAt])
}

model ResearchDataset {
  id          String    @id @default(cuid())
  name        String
  description String?
  dataType    String
  filters     Json?
  recordCount Int       @default(0)
  anonymized  Boolean   @default(true)
  requestedBy String?
  approvedBy  String?
  status      String    @default("PENDING")
  exportedAt  DateTime?
  expiresAt   DateTime?
  createdAt   DateTime  @default(now())
  metadata    Json?

  @@index([status])
  @@index([dataType])
  @@index([createdAt])
}

model CommentConversion {
  id               String    @id @default(cuid())
  commentId        String
  doctorId         String
  patientId        String
  postId           String
  profileVisited   Boolean   @default(false)
  messageClicked   Boolean   @default(false)
  visitedAt        DateTime?
  messageClickedAt DateTime?
  createdAt        DateTime  @default(now())
  comment          Comment   @relation(fields: [commentId], references: [id], onDelete: Cascade)
  doctor           User      @relation("DoctorConversions", fields: [doctorId], references: [id], onDelete: Cascade)
  patient          User      @relation("PatientConversions", fields: [patientId], references: [id], onDelete: Cascade)
  post             Post      @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([commentId])
  @@index([doctorId])
  @@index([patientId])
  @@index([messageClicked])
}

model PatientFeedback {
  id                           String        @id @default(cuid())
  patientId                    String
  doctorId                     String
  conversationId               String?
  appointmentId                String?
  status                       String        @default("PENDING")
  feedbackCount                Int           @default(0)
  lastFeedbackAt               DateTime?
  curedAt                      DateTime?
  wasClinicVisit               Boolean       @default(false)
  createdAt                    DateTime      @default(now())
  updatedAt                    DateTime      @updatedAt
  communicationRating          Float?
  feedback                     String?
  isAnonymous                  Boolean       @default(false)
  professionalismRating        Float?
  rating                       Float?
  treatmentEffectivenessRating Float?
  appointment                  Appointment?  @relation(fields: [appointmentId], references: [id])
  conversation                 Conversation? @relation(fields: [conversationId], references: [id])
  doctor                       User          @relation("DoctorFeedbacks", fields: [doctorId], references: [id], onDelete: Cascade)
  patient                      User          @relation("PatientFeedbacks", fields: [patientId], references: [id], onDelete: Cascade)

  @@index([patientId])
  @@index([doctorId])
  @@index([status])
  @@index([lastFeedbackAt])
  @@index([rating])
}

model CommunityActivity {
  id                 String    @id @default(cuid())
  communityId        String    @unique
  activityTier       String    @default("INACTIVE")
  totalPosts         Int       @default(0)
  totalComments      Int       @default(0)
  totalMembers       Int       @default(0)
  avgPostsPerDay     Float     @default(0)
  avgCommentsPerPost Float     @default(0)
  lastActivityAt     DateTime?
  calculatedAt       DateTime  @default(now())
  community          Community @relation(fields: [communityId], references: [id], onDelete: Cascade)

  @@index([activityTier])
}

model DoctorActivityMetrics {
  id                    String    @id @default(cuid())
  doctorId              String    @unique
  totalPatientsAcquired Int       @default(0)
  avgReplyTimeHours     Float?
  lastActiveAt          DateTime?
  dailyActivityPattern  Json?
  weeklyActivityPattern Json?
  monthlyGrowth         Json?
  calculatedAt          DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  averageRating         Float?
  totalRatings          Int       @default(0)
  doctor                User      @relation(fields: [doctorId], references: [id], onDelete: Cascade)

  @@index([doctorId])
  @@index([lastActiveAt])
  @@index([averageRating])
}

model UserActivityLog {
  id           String   @id @default(cuid())
  userId       String
  activityType String
  metadata     Json?
  hourOfDay    Int
  dayOfWeek    Int
  createdAt    DateTime @default(now())
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([activityType])
  @@index([createdAt])
  @@index([hourOfDay])
  @@index([dayOfWeek])
}

model PostPriority {
  id               String   @id @default(cuid())
  postId           String   @unique
  priorityLevel    String
  urgencyScore     Float    @default(0)
  detectedSymptoms Json?
  calculatedAt     DateTime @default(now())
  post             Post     @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([priorityLevel])
  @@index([urgencyScore])
}

model PatientHealthProfile {
  id                      String    @id @default(cuid())
  userId                  String    @unique
  ageGroup                String?
  biologicalSex           String?
  bloodGroup              String?
  preExistingConditions   Json?
  currentMedications      Json?
  allergies               Json?
  smokingStatus           String?
  alcoholConsumption      String?
  activityLevel           String?
  sleepHours              String?
  primaryHealthConcern    String?
  secondaryHealthConcerns Json?
  completedAt             DateTime?
  lastUpdatedAt           DateTime?
  consentForDoctorView    Boolean   @default(true)
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt
  user                    User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model GeographicHealthTrend {
  id           String   @id @default(cuid())
  symptom      String
  pincode      String?
  city         String?
  district     String?
  state        String?
  country      String   @default("India")
  reportCount  Int      @default(1)
  severity     String?
  timeWindow   String
  calculatedAt DateTime @default(now())
  periodStart  DateTime
  periodEnd    DateTime

  @@index([symptom])
  @@index([pincode])
  @@index([city])
  @@index([district])
  @@index([state])
  @@index([timeWindow])
  @@index([calculatedAt])
}

model HealthProfile {
  id                    String     @id @default(cuid())
  userId                String     @unique
  ageGroup              String?
  biologicalSex         String?
  nationality           String?
  weightRange           String?
  heightRange           String?
  activityLevel         String?
  medicalConditions     Json?
  currentMedications    Json?
  foodAllergies         Json?
  dietType              String?
  religiousRestrictions String?
  foodsToAvoid          String?
  cookingAccess         String?
  primaryGoal           String?
  sleepHours            String?
  waterIntake           String?
  completedAt           DateTime?
  createdAt             DateTime   @default(now())
  updatedAt             DateTime   @updatedAt
  riskLevel             String?
  dietPlans             DietPlan[]
  user                  User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model DietPlan {
  id               String        @id @default(cuid())
  userId           String
  healthProfileId  String
  dailyCalorieGoal Int
  planData         Json
  nutritionalInfo  Json
  dietaryNote      String?
  generatedAt      DateTime      @default(now())
  savedAt          DateTime?
  isActive         Boolean       @default(false)
  healthProfile    HealthProfile @relation(fields: [healthProfileId], references: [id], onDelete: Cascade)
  user             User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([healthProfileId])
  @@index([generatedAt])
}

model DoctorEndorsement {
  id        String   @id @default(cuid())
  postId    String
  doctorId  String
  createdAt DateTime @default(now())
  doctor    User     @relation("DoctorEndorsements", fields: [doctorId], references: [id], onDelete: Cascade)
  post      Post     @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@unique([postId, doctorId])
  @@index([postId])
  @@index([doctorId])
}

model SymptomHeatmapStat {
  id          String   @id @default(uuid())
  regionType  String
  regionName  String
  symptomTag  String
  caseCount   Int      @default(0)
  severityAvg Float    @default(0)
  spikeRatio  Float    @default(1)
  alertLevel  String   @default("none")
  source      String
  reportDate  DateTime @db.Date
  createdAt   DateTime @default(now())

  @@unique([regionType, regionName, symptomTag, reportDate])
  @@index([regionType, regionName, reportDate])
  @@index([symptomTag, reportDate])
}

model MedicalVerification {
  id              String   @id @default(cuid())
  postId          String?
  commentId       String?
  content         String
  isAccurate      Boolean
  confidenceScore Float
  concerns        Json
  recommendations Json
  riskLevel       String
  requiresReview  Boolean  @default(false)
  verifiedAt      DateTime @default(now())
  verifiedBy      String?
  metadata        Json?

  @@index([postId])
  @@index([commentId])
  @@index([riskLevel])
  @@index([requiresReview])
  @@index([verifiedAt])
}

model ContentModeration {
  id            String   @id @default(cuid())
  content       String
  authorId      String
  contentType   String
  isAppropriate Boolean
  toxicityScore Float
  categories    Json
  action        String
  confidence    Float
  moderatedAt   DateTime @default(now())
  moderatedBy   String?
  metadata      Json?
  author        User     @relation(fields: [authorId], references: [id])

  @@index([authorId])
  @@index([contentType])
  @@index([action])
  @@index([moderatedAt])
}

model AutoFlag {
  id        String   @id @default(cuid())
  postId    String?
  commentId String?
  reason    String
  status    String   @default("PENDING")
  flaggedAt DateTime @default(now())
  metadata  Json?

  @@index([postId])
  @@index([commentId])
  @@index([status])
  @@index([flaggedAt])
}

model LiabilityWaiver {
  id              String   @id @default(cuid())
  doctorId        String
  patientId       String
  interactionType String
  waiverText      String
  acceptedAt      DateTime
  ipAddress       String?
  userAgent       String?
  metadata        Json?

  @@index([doctorId])
  @@index([patientId])
  @@index([interactionType])
  @@index([acceptedAt])
}

model UserDevice {
  id               String   @id @default(cuid())
  userId           String
  fcmToken         String
  deviceType       String
  isActive         Boolean  @default(true)
  subscriptionData Json?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  user             User     @relation(fields: [userId], references: [id])

  @@unique([userId, fcmToken])
  @@index([userId])
  @@index([isActive])
}

model SearchHistory {
  id             String   @id @default(cuid())
  userId         String
  query          String
  type           String
  searchCount    Int      @default(1)
  lastSearchedAt DateTime @default(now())
  user           User     @relation(fields: [userId], references: [id])

  @@unique([userId, query, type])
  @@index([userId])
  @@index([type])
  @@index([lastSearchedAt])
}

model BackupRecord {
  id              String    @id @default(cuid())
  backupId        String    @unique
  type            String
  status          String
  totalSize       BigInt?
  duration        Int?
  recordsBackedUp Int?
  metadata        Json?
  createdAt       DateTime  @default(now())
  completedAt     DateTime?
  error           String?

  @@index([backupId])
  @@index([type])
  @@index([status])
  @@index([createdAt])
}

model PerformanceMetric {
  id        String   @id @default(cuid())
  name      String
  value     Float
  unit      String
  metadata  Json?
  timestamp DateTime @default(now())

  @@index([name])
  @@index([timestamp])
}

model HealthCheck {
  id        String   @id @default(cuid())
  overall   String
  services  Json
  timestamp DateTime @default(now())

  @@index([overall])
  @@index([timestamp])
}

model PerformanceAlert {
  id         String    @id @default(cuid())
  type       String
  details    Json
  resolved   Boolean   @default(false)
  createdAt  DateTime  @default(now())
  resolvedAt DateTime?

  @@index([type])
  @@index([resolved])
  @@index([createdAt])
}

model EmergencyBroadcast {
  id             String    @id @default(cuid())
  title          String
  message        String
  priority       String    @default("MEDIUM")
  type           String    @default("SYSTEM")
  targetAudience String    @default("ALL")
  targetRegion   String?
  isActive       Boolean   @default(true)
  expiresAt      DateTime?
  createdBy      String
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  creator        User      @relation("EmergencyBroadcastCreator", fields: [createdBy], references: [id])

  @@index([isActive])
  @@index([priority])
  @@index([createdAt])
  @@index([expiresAt])
}

model SupportGroup {
  id          String   @id @default(cuid())
  name        String
  condition   String
  description String
  isPrivate   Boolean  @default(false)
  moderators  Json
  members     Json
  memberCount Int      @default(0)
  rules       Json?
  meetups     Json?
  resources   Json?
  createdBy   String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([condition])
  @@index([isPrivate])
  @@index([memberCount])
}

model SupportGroupPost {
  id           String   @id @default(cuid())
  groupId      String
  authorId     String
  isAnonymous  Boolean  @default(false)
  title        String
  content      String
  type         String
  upvotes      Int      @default(0)
  commentCount Int      @default(0)
  isPinned     Boolean  @default(false)
  createdAt    DateTime @default(now())

  @@index([groupId])
  @@index([authorId])
  @@index([type])
  @@index([createdAt])
}

model ForumQuestion {
  id               String        @id @default(cuid())
  title            String
  content          String
  authorId         String
  category         String?
  tags             String[]
  status           String        @default("OPEN")
  upvotes          Int           @default(0)
  viewCount        Int           @default(0)
  isPinned         Boolean       @default(false)
  isVerified       Boolean       @default(false)
  acceptedAnswerId String?
  createdAt        DateTime      @default(now())
  updatedAt        DateTime      @updatedAt
  downvotes        Int           @default(0)
  isAnonymous      Boolean       @default(false)
  answers          ForumAnswer[]
  author           User          @relation("ForumQuestions", fields: [authorId], references: [id])

  @@index([authorId])
  @@index([category])
  @@index([status])
  @@index([createdAt])
  @@index([upvotes])
}

model ForumAnswer {
  id         String        @id @default(cuid())
  questionId String
  authorId   String
  content    String
  upvotes    Int           @default(0)
  isAccepted Boolean       @default(false)
  isVerified Boolean       @default(false)
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt
  downvotes  Int           @default(0)
  author     User          @relation("ForumAnswers", fields: [authorId], references: [id])
  question   ForumQuestion @relation(fields: [questionId], references: [id], onDelete: Cascade)

  @@index([questionId])
  @@index([authorId])
  @@index([isAccepted])
  @@index([createdAt])
}

model SuccessStory {
  id           String         @id @default(cuid())
  authorId     String
  title        String
  condition    String
  story        String
  beforePhotos Json?
  afterPhotos  Json?
  treatment    String?
  duration     String?
  likes        Int            @default(0)
  views        Int            @default(0)
  status       String         @default("PENDING")
  isVerified   Boolean        @default(false)
  isInspiring  Boolean        @default(false)
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  comments     StoryComment[]
  likedBy      StoryLike[]
  author       User           @relation("SuccessStories", fields: [authorId], references: [id])

  @@index([authorId])
  @@index([condition])
  @@index([status])
  @@index([createdAt])
  @@index([likes])
}

model StoryLike {
  id        String       @id @default(cuid())
  storyId   String
  userId    String
  createdAt DateTime     @default(now())
  story     SuccessStory @relation(fields: [storyId], references: [id], onDelete: Cascade)

  @@unique([storyId, userId])
  @@index([storyId])
  @@index([userId])
}

model StoryComment {
  id        String       @id @default(cuid())
  storyId   String
  authorId  String
  content   String
  createdAt DateTime     @default(now())
  author    User         @relation("StoryComments", fields: [authorId], references: [id], onDelete: Cascade)
  story     SuccessStory @relation(fields: [storyId], references: [id], onDelete: Cascade)

  @@index([storyId])
  @@index([authorId])
}

model ChallengeParticipant {
  id          String    @id @default(cuid())
  challengeId String
  userId      String
  progress    Float     @default(0)
  isCompleted Boolean   @default(false)
  completedAt DateTime?
  joinedAt    DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  approvedAt  DateTime?
  approvedBy  String?
  isApproved  Boolean   @default(true)
  points      Int       @default(0)

  @@unique([challengeId, userId])
  @@index([challengeId])
  @@index([userId])
  @@index([isCompleted])
}

model ChallengeApprovalRequest {
  id          String    @id @default(cuid())
  challengeId String
  patientId   String
  doctorId    String
  status      String    @default("PENDING")
  notes       String?
  createdAt   DateTime  @default(now())
  respondedAt DateTime?

  @@unique([challengeId, patientId])
  @@index([patientId])
  @@index([doctorId])
  @@index([status])
}

model HealthChallenge {
  id                     String   @id @default(cuid())
  title                  String
  description            String
  type                   String
  goal                   Int
  unit                   String
  participants           Json
  startDate              DateTime
  endDate                DateTime
  rewards                Json
  leaderboard            Json
  isActive               Boolean  @default(true)
  createdBy              String?
  location               String?
  participantCount       Int      @default(0)
  duration               String?
  category               String?
  difficulty             String?
  createdAt              DateTime @default(now())
  requiresDoctorApproval Boolean  @default(false)
  riskLevel              String   @default("LOW")
  approvedByDoctors      Json     @default("[]")
  isDoctorApproved       Boolean  @default(false)

  @@index([type])
  @@index([isActive])
  @@index([startDate])
  @@index([endDate])
  @@index([riskLevel])
  @@index([isDoctorApproved])
}

enum UserRole {
  PATIENT
  DOCTOR
  NURSE
  MEDICAL_STUDENT
  PHARMACIST
  COMMUNITY_CONTRIBUTOR
  MODERATOR
  ADMIN
}

enum DoctorVerificationStatus {
  PENDING
  UNDER_REVIEW
  APPROVED
  REJECTED
  SUSPENDED
}

enum PostType {
  TEXT
  IMAGE
  VIDEO
  LINK
  POLL
  GALLERY
  LIVE
}

enum SortType {
  HOT
  NEW
  TOP
  RISING
  CONTROVERSIAL
  BEST
}

enum AppointmentStatus {
  PENDING
  APPROVED
  REJECTED
  COMPLETED
  CANCELLED
}

enum MessageType {
  TEXT
  IMAGE
  FILE
}

enum AuditAction {
  ADMIN_LOGIN
  ADMIN_LOGOUT
  USER_SUSPEND
  USER_UNSUSPEND
  USER_DELETE
  USER_UPDATE
  POST_DELETE
  POST_RESTORE
  POST_PIN
  POST_UNPIN
  POST_LOCK
  POST_UNLOCK
  COMMENT_DELETE
  COMMENT_RESTORE
  REPORT_RESOLVE
  REPORT_DISMISS
  DOCTOR_APPROVE
  DOCTOR_REJECT
  DOCTOR_SUSPEND
  SETTINGS_UPDATE
  ROLE_CHANGE
}

enum ContentType {
  POST
  COMMENT
  APPOINTMENT
  COMMUNITY
}

enum NotificationType {
  REPLY
  MENTION
  AWARD
  FOLLOWER
  APPOINTMENT_REQUEST
  APPOINTMENT_UPDATE
  VERIFICATION_STATUS
  COMMUNITY_INVITE
  DIRECT_MESSAGE
  SYSTEM_ANNOUNCEMENT
  UPVOTE_MILESTONE
}

enum PaymentStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  REFUNDED
  CANCELLED
}

enum PaymentType {
  CONSULTATION
  SUBSCRIPTION
  PREMIUM_FEATURE
}

enum RefundStatus {
  PENDING
  APPROVED
  REJECTED
  PROCESSED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
  PAST_DUE
}

// Disease Trends Cache for Web Search Results
model DiseaseTrendsCache {
  id           String   @id @default(cuid())
  disease      String
  location     String
  year         Int
  searchQuery  String
  data         Json
  sources      Json?
  lastUpdated  DateTime @default(now())
  expiresAt    DateTime
  createdAt    DateTime @default(now())

  @@index([disease, location, year])
  @@index([expiresAt])
}
